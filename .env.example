# =============================================================================
# Domain Configuration (REQUIRED - customize for your environment)
# =============================================================================
# Base domain for all services (e.g., example.com, mycompany.local)
BASE_DOMAIN=example.com

# Service subdomains (automatically prefixed to BASE_DOMAIN)
# Production: https://blacklist.${BASE_DOMAIN}
# Development: https://blacklist-dev.${BASE_DOMAIN}
# Registry: https://registry.${BASE_DOMAIN}
# GitLab: https://gitlab.${BASE_DOMAIN}
# Portainer: https://portainer.${BASE_DOMAIN}

# Full URLs (auto-generated from BASE_DOMAIN, override if needed)
# PROD_URL=https://blacklist.${BASE_DOMAIN}
# DEV_URL=https://blacklist-dev.${BASE_DOMAIN}
# REGISTRY_URL=https://registry.${BASE_DOMAIN}
# GITLAB_URL=https://gitlab.${BASE_DOMAIN}

# =============================================================================
# Database Configuration
# =============================================================================
POSTGRES_PASSWORD=your_postgres_password

# REGTECH Authentication (Korean Financial Security Institute)
REGTECH_ID=your_regtech_username
REGTECH_PW=your_regtech_password
REGTECH_BASE_URL=https://regtech.fsec.or.kr

# SECUDIUM Authentication (Threat Intelligence Provider - Added 2025-10-21)
SECUDIUM_ID=your_secudium_username
SECUDIUM_PW=your_secudium_password
SECUDIUM_BASE_URL=https://rest.secudium.net

# Application Environment (optional)
FLASK_ENV=production
FLASK_DEBUG=0

# Security Configuration (Phase 1.3: CSRF & Rate Limiting)
# CRITICAL: Set a strong secret key in production
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=change-me-flask-secret-key-minimum-32-characters

# =============================================================================
# Encryption Keys (REQUIRED for credential storage)
# =============================================================================
# Master Key for Credential Encryption (AES-256 via PBKDF2)
# Used by: app (secure_credential_service), collector (database.py)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
CREDENTIAL_MASTER_KEY=change-me-credential-master-key-minimum-32-chars

# Application Secret Key (used for session signing, CSRF tokens)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-app-secret-key-minimum-32-characters

# Encryption Salt (used with CREDENTIAL_MASTER_KEY for key derivation)
# Default: blacklist-regtech-salt-2025 (if not set)
# Generate with: python -c "import secrets; print(secrets.token_hex(16))"
ENCRYPTION_SALT=blacklist-regtech-salt-2025

# Legacy Credential Encryption Key (Fernet direct key, deprecated)
# Only needed if migrating from pre-v3.5 credential storage
# CREDENTIAL_ENCRYPTION_KEY=

# Redis Configuration (for rate limiting and caching)
REDIS_HOST=blacklist-redis
REDIS_PORT=6379

# Development Volume Mounts (optional)
# Uncomment for development with live code reload:
# APP_SOURCE_MOUNT=./app
# APP_LOGS_MOUNT=./app/logs

# Cloudflare WARP Configuration (Optional - for Playwright collection only)
# Note: WARP is enabled in code, NOT in air-gap package
# Install WARP: curl -fsSL https://pkg.cloudflareclient.com/install.sh | sudo bash
# Setup: warp-cli register && warp-cli connect
# Verify SOCKS5: curl -x socks5://127.0.0.1:40000 https://cloudflare.com/cdn-cgi/trace
# WARP_ENABLED=true
# WARP_SOCKS5_HOST=127.0.0.1
# WARP_SOCKS5_PORT=40000
# Format: http://host:port or socks5://host:port
WARP_PROXY_URL=