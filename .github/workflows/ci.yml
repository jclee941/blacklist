# Consolidated CI Pipeline
# Replaces: ci.yml (frontend-only) + ci-new.yml (self-hosted only)
# Fixes: tsc error suppression (|| echo), vitest continue-on-error, missing backend CI
# Runner: Set vars.RUNNER to 'self-hosted' in repo settings for self-hosted runner
name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read
  packages: write

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  detect-changes:
    runs-on: ${{ vars.RUNNER || 'ubuntu-latest' }}
    outputs:
      frontend: ${{ steps.filter.outputs.frontend }}
      backend: ${{ steps.filter.outputs.backend }}
      collector: ${{ steps.filter.outputs.collector }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            frontend:
              - 'frontend/**'
            backend:
              - 'app/**'
              - 'tests/**'
            collector:
              - 'collector/**'

  # Replaces inline lint-frontend with reusable workflow
  lint-frontend:
    needs: detect-changes
    if: needs.detect-changes.outputs.frontend == 'true'
    uses: qws941/.github/.github/workflows/_ci-node.yml@main
    with:
      node-version: "20"
      working-directory: frontend
      run-lint: true
      run-typecheck: true
    secrets: inherit

  lint-backend:
    needs: detect-changes
    if: >-
      needs.detect-changes.outputs.backend == 'true' ||
      needs.detect-changes.outputs.collector == 'true'
    runs-on: ${{ vars.RUNNER || 'ubuntu-latest' }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip
      - run: pip install ruff==0.9.10
      - run: ruff check app/ collector/
      - run: ruff format --check app/ collector/

  test-frontend:
    needs: [detect-changes, lint-frontend]
    if: >-
      always() && !failure() && !cancelled() &&
      needs.detect-changes.outputs.frontend == 'true'
    runs-on: ${{ vars.RUNNER || 'ubuntu-latest' }}
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - name: Run tests with coverage
        run: npm run test -- --coverage
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-coverage
          path: frontend/coverage/
          retention-days: 30

  test-backend:
    needs: [detect-changes, lint-backend]
    if: >-
      always() && !failure() && !cancelled() &&
      needs.detect-changes.outputs.backend == 'true'
    runs-on: ${{ vars.RUNNER || 'ubuntu-latest' }}
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: blacklist
          POSTGRES_PASSWORD: blacklist
          POSTGRES_DB: blacklist_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U blacklist"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd="redis-cli ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    env:
      DATABASE_URL: postgresql://blacklist:blacklist@localhost:5432/blacklist_test
      REDIS_URL: redis://localhost:6379/0
      POSTGRES_HOST: localhost
      POSTGRES_PORT: "5432"
      POSTGRES_DB: blacklist_test
      POSTGRES_USER: blacklist
      POSTGRES_PASSWORD: blacklist
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip
      - run: pip install -r app/requirements.txt pytest-cov
      - name: Run tests with coverage
        run: |
          python -m pytest tests/unit --ignore=tests/unit/collector -v \
            --cov=app/core \
            --cov-report=term-missing \
            --cov-report=xml:coverage.xml \
            --cov-fail-under=0
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-coverage
          path: coverage.xml
          retention-days: 30

  test-collector:
    needs: [detect-changes, lint-backend]
    if: >-
      always() && !failure() && !cancelled() &&
      needs.detect-changes.outputs.collector == 'true'
    runs-on: ${{ vars.RUNNER || 'ubuntu-latest' }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip
      - run: pip install -r collector/requirements.txt pytest-cov
      - name: Run collector tests with coverage
        env:
          SECUDIUM_EMAIL: ${{ secrets.SECUDIUM_EMAIL }}
          SECUDIUM_EMAIL_PASSWORD: ${{ secrets.SECUDIUM_EMAIL_PASSWORD }}
          PYTHONPATH: collector
        run: |
          python -m pytest tests/unit/collector -v \
            --cov=collector/core \
            --cov-report=term-missing \
            --cov-report=xml:collector-coverage.xml
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: collector-coverage
          path: collector-coverage.xml
          retention-days: 30

  build:
    needs: [detect-changes, lint-frontend, lint-backend, test-frontend, test-backend, test-collector]
    if: >-
      always() && !failure() && !cancelled() &&
      (needs.detect-changes.outputs.frontend == 'true' ||
       needs.detect-changes.outputs.backend == 'true' ||
       needs.detect-changes.outputs.collector == 'true')
    runs-on: ${{ vars.RUNNER || 'ubuntu-latest' }}
    strategy:
      fail-fast: false
      matrix:
        service: [frontend, app, collector]
        include:
          - service: frontend
            context: .
            dockerfile: frontend/Dockerfile
          - service: app
            context: .
            dockerfile: app/Dockerfile
          - service: collector
            context: .
            dockerfile: collector/Dockerfile
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          load: true
          tags: blacklist-${{ matrix.service }}:ci
          build-args: |
            APP_VERSION=ci
            GIT_COMMIT=${{ github.sha }}
            BUILD_DATE=${{ github.event.head_commit.timestamp }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - run: docker save blacklist-${{ matrix.service }}:ci | gzip > ${{ matrix.service }}.tar.gz
      - uses: actions/upload-artifact@v4
        with:
          name: image-${{ matrix.service }}
          path: ${{ matrix.service }}.tar.gz
          retention-days: 1

  e2e:
    needs: build
    if: needs.build.result == 'success'
    runs-on: ${{ vars.RUNNER || 'ubuntu-latest' }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          pattern: image-*
          merge-multiple: true
      - name: Load images
        run: |
          for f in *.tar.gz; do
            gunzip -c "$f" | docker load
          done
      - name: Start services
        run: docker compose -f .github/docker-compose.ci.yml up -d --wait --wait-timeout 120
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - working-directory: frontend
        run: npm ci
      - working-directory: frontend
        run: npx playwright install --with-deps chromium
      - name: Run E2E tests
        working-directory: frontend
        run: npm run test:e2e
        env:
          PLAYWRIGHT_BASE_URL: https://localhost:3443
      - name: Collect logs
        if: failure()
        run: docker compose -f .github/docker-compose.ci.yml logs --no-color > docker-logs.txt 2>&1
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: e2e-failure-logs
          path: |
            docker-logs.txt
            frontend/test-results/
          retention-days: 7
      - if: always()
        run: docker compose -f .github/docker-compose.ci.yml down -v

  push-images:
    needs: e2e
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'
    uses: ./.github/workflows/build-images.yml
    with:
      version: latest
      push: true
    secrets: inherit

